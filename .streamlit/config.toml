[server]
maxUploadSize = 20
headless = true
# Behind the nginx reverse proxy on the server these two are turned off so the
# app loads reliably. (Locally it makes no difference.)
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false

[theme]
base = "light"
